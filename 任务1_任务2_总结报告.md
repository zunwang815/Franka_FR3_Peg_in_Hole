# Franka FR3 Peg-in-Hole 项目最终总结报告

> 日期：2026-08-19  
> 仿真平台：Isaac Lab / Isaac Sim 4.5  
> 控制器：6D Pose Operational Space Control  
> 强化学习：RSL-RL PPO

## 一、最终结论

老师要求的任务1和任务2均已完成当前仿真验收，并且使用了 RL。

| 任务 | 最终方法 | 严格评估结果 |
|---|---|---:|
| 任务1：单孔插入 | 几何教师 + residual PPO | 基础协议 512/512 = 100%；强扰动协议 PPO 124/128 = 96.88% |
| 任务2：六孔阵列 | 几何教师 + residual PPO | PPO 128/128 = 100% |

任务2最终公平对照中，几何教师为 `110/128 = 85.94%`，改进后的 PPO 为 `128/128 = 100%`，PPO 提升 `14.06` 个百分点。

所有主线验收均不加入控制器动作延迟。老师的任务文件没有提出延迟噪声要求，且实验表明该控制器对延迟高度敏感，因此延迟没有混入最终协议。

上述结论仅针对 Isaac Lab 仿真，不等同于真实 FR3 机器人的实机成功率。

## 二、老师任务与成功判据

原始任务要求整理如下：

1. 使用 Franka FR3 构建 Peg-in-Hole 物理仿真；
2. 将直径约 20 mm 的圆棒插入直径 23 mm 的孔；
3. 目标孔在约 `10 cm × 10 cm` 的可达范围内随机；
4. 任务1建模棒相对 flange 的约 ±5 mm 抓取不确定性；
5. 任务2增加 2×3 个孔，孔间距 30 mm，支持任意指定目标孔；
6. 机器人不能直接使用无噪声的绝对棒/孔位姿。

统一的最终成功判据为：

- 径向误差 `<= 1.3 mm`；
- 插入深度 `15–40 mm`；
- 棒倾角 `<= 2°`；
- 未发生 workspace violation；
- 在 20 秒，即 600 个控制步内完成。

成功、timeout、over-insertion 和其他失败均单独统计，不能把一般的 `terminated` 直接当成成功。

## 三、总体方法论

项目采用以下推进路线：

```text
需求拆解
  → 物理几何和 termination 审计
  → 几何教师可达性验证
  → 加入孔位、抓取和执行扰动
  → 冻结几何教师并训练有界 residual PPO
  → 固定 seed 的批量评估
  → 按失败原因和偏移分组复核
  → Isaac Sim 回放与最终报告
```

### 3.1 物理模型与几何教师

任务使用 20 mm 圆棒、23 mm 孔和 36 段环形孔壁碰撞结构。几何教师根据棒尖到孔中心的相对位移、插入深度、棒倾角和姿态误差生成 6D Pose/OSC 动作。

教师的作用是提供可解释、可审计的稳定动作方向，并首先验证“当前机器人、孔壁和控制器是否物理可达”。它不是 RL。

### 3.2 相对观测与噪声建模

策略观测主要包括：

- 9 维关节位置；
- 9 维关节速度；
- 3 维棒尖到目标孔的相对位移；
- 3 维棒倾斜信息；
- 任务2的 6 维目标孔 one-hot；
- 6 维上一动作。

任务2的观测总维度为 36。策略不读取隐藏的 mount 关节，也不直接读取绝对孔位姿。

最终压力协议对每个 episode 固定采样 `0.5 mm` 的 XY 孔位估计偏差，并保留每步动作噪声和每 episode 控制增益扰动。动作延迟始终为 0。

### 3.3 Residual PPO

最终 RL 结构为：

```text
最终动作 = 冻结的几何教师动作 + 有界 residual action
```

residual head 为 `64-64-Tanh` 网络，residual scale 为 `0.15`。PPO 通过 teacher-action residual penalty 限制策略远离几何教师，学习的是对执行扰动、目标孔条件和阵列状态的局部修正，而不是从零学习完整插入行为。

任务2最终改进训练采用任务1中表现稳定的配置：

| 参数 | 设置 |
|---|---:|
| 并行环境 | 1024 |
| 训练轮数 | 500 |
| rollout | 64 steps/env |
| learning rate | `1e-5` |
| PPO epochs | 1 |
| 初始策略噪声 | `0.03` |
| entropy coefficient | 0 |
| residual penalty | 100 |
| 训练动作噪声 | `0.02` |
| 动作增益扰动 | 5% |

实验表明，任务2先前使用 `learning rate=1e-4`、两轮 PPO epoch、初始噪声 `0.005` 和 residual penalty `10` 的配置，在强动作噪声下只能达到约 50.78%。该对照训练保留在 [`runs/ppo/custom/20260818_212804/`](runs/ppo/custom/20260818_212804/)；改用任务1的保守更新和较强初始探索后，最终提升到 100%。

## 四、任务1：单孔插入

### 4.1 环境

任务1使用单孔 23 mm 几何，目标孔在约 `10 cm × 10 cm` 范围内随机。训练和验证过程中逐级加入 Peg mount X/Y 抓取偏差、孔位估计误差和执行扰动。

### 4.2 结果

在基础严格协议下，几何教师和 residual PPO 均达到 `512/512 = 100%`，没有 timeout 或 over-insertion。

基础协议的完整训练目录为 [`runs/ppo/hole20_reward/20260818_130108/`](runs/ppo/hole20_reward/20260818_130108/)，批量结果同时记录于 [`Day6_工作记录.md`](Day6_工作记录.md)。

在用于验证 RL 鲁棒性增益的强扰动协议中：

| 方法 | 成功率 | timeout | over-insertion |
|---|---:|---:|---:|
| 几何教师 | 119/128 = 92.97% | 9 | 0 |
| PPO 500 轮 | 124/128 = 96.88% | 4 | 0 |

因此任务1不仅满足 90% 要求，而且在强扰动测试中 PPO 超过了几何教师。

任务1强扰动训练配置和结果记录见 [`runs/ppo/stress/custom/20260818_164739/run_config.json`](runs/ppo/stress/custom/20260818_164739/run_config.json) 与 [`runs/eval/stress_rl_gain_hole05_bias_500_summary.md`](runs/eval/stress_rl_gain_hole05_bias_500_summary.md)。

## 五、任务2：2×3 六孔阵列

### 5.1 阵列环境

- 2 行 × 3 列，共 6 个孔；
- 孔中心间距 30 mm；
- 阵列整体在约 `10 cm × 10 cm` 范围内随机；
- 目标孔随机选择或固定指定 0–5；
- 六孔均保留真实碰撞几何；
- 使用与任务1一致的 fixed-Peg 动力学链和 6D OSC 接口。

任务2不是简单地重复六次任务1。策略必须同时处理目标孔条件化、阵列整体坐标变化和非目标孔的共存碰撞环境，因此加入了目标孔 one-hot，并保持六孔之间的相对几何结构不变。

### 5.2 早期失败与修正

早期六孔实验的 `1.56%/2.34%` 结果来自旧诊断协议，不能作为最终任务2结论。旧协议存在：

- 阵列默认 episode 只有 12 秒，即约 360 步；
- 几何教师插入门限只有 1 mm；
- 评估路径和最终公平 wrapper 路径不一致。

在统一为 20 秒、2 mm 教师门限并重新建立几何教师基线后，任务2的物理可达性得到验证。

早期诊断证据见 [`runs/eval/task2_stress_hole05_summary.md`](runs/eval/task2_stress_hole05_summary.md) 与 [`runs/eval/task2_stress_hole05_bias_500_summary.md`](runs/eval/task2_stress_hole05_bias_500_summary.md)。无强扰动时的六孔 `512/512` 基础验收模型及可视化来源保留在 [`runs/ppo/custom/20260818_141826/`](runs/ppo/custom/20260818_141826/)。

### 5.3 最终公平协议

| 项目 | 设置 |
|---|---|
| 评估回合 | 128 |
| 并行环境 | 64 |
| seed | 42 |
| episode | 20 秒 / 600 步 |
| 固定孔位估计偏差 | XY 标准差 0.5 mm |
| 动作噪声 | `0.0305` |
| 动作增益扰动 | 5% |
| 动作延迟 | 0 步 |
| 教师 alignment gate | 2 mm |
| 零空间刚度 | 20 |
| 深度安全屏障 | 关闭 |

### 5.4 最终结果

| 方法 | 成功 | timeout | over-insertion | 成功率 |
|---|---:|---:|---:|---:|
| 几何教师 | 110/128 | 18 | 0 | 85.94% |
| 改进 PPO `model_499` | 128/128 | 0 | 0 | 100% |

PPO 相对几何教师提升 `14.06` 个百分点，达到了“至少不低于几何教师”的目标，并在该公平协议下超过了几何教师。

PPO 终端指标为：

- 插入深度：均值 16.461 mm，范围 15.012–26.465 mm；
- 径向误差：均值 0.836 mm，最大 1.294 mm；
- 倾角：均值 0.736°，最大 1.612°。

全部 128 个 episode 均满足最终成功判据。

## 六、评估链路与可复现性

最终 PPO 评估使用 [`scripts/eval_checkpoint_simple.py`](scripts/eval_checkpoint_simple.py) 的 `RslRlVecEnvWrapper` 路径。此前新增的直接 raw-env 评估路径会在第二个 OSC 控制步触发 Isaac 底层提前退出，因此不再作为正式评估入口。

这次排查确认：

1. 检查点本身可以正常加载；
2. 旧模型和新模型都能在 wrapper 路径下正常评估；
3. 评估失败来自 raw-env 评估入口的环境初始化/观测动作链路差异，而不是奖励函数或检查点损坏；
4. 正式结果必须记录评估入口、时间预算、门限和扰动参数，避免不同协议之间直接比较。

## 七、可视化与证据文件

主要产物如下：

- 任务2最终训练配置：[`runs/ppo/custom/20260818_232714/run_config.json`](runs/ppo/custom/20260818_232714/run_config.json)；
- 任务2最终 checkpoint：[`runs/ppo/custom/20260818_232714/model_499.pt`](runs/ppo/custom/20260818_232714/model_499.pt)；
- 任务2最终公平评估：[`runs/eval/task2_fair_ppo_task1ppo_action02_eval128.json`](runs/eval/task2_fair_ppo_task1ppo_action02_eval128.json)；
- 几何教师公平基线：[`runs/eval/task2_fair_teacher_action0305_gate2_128.json`](runs/eval/task2_fair_teacher_action0305_gate2_128.json)；
- 任务2可视化目录：[`artifacts/task2_visualization/`](artifacts/task2_visualization/)；
- 任务1/任务2最终结果总览图：[`artifacts/task2_visualization/final_results_summary.png`](artifacts/task2_visualization/final_results_summary.png)；
- 任务1强扰动结果：[`runs/eval/stress_rl_gain_hole05_bias_500_summary.md`](runs/eval/stress_rl_gain_hole05_bias_500_summary.md)；
- 共享几何教师：[`scripts/geometric_teacher.py`](scripts/geometric_teacher.py)；
- residual 策略：[`scripts/residual_policy.py`](scripts/residual_policy.py)；
- 动作扰动：[`scripts/action_perturbation.py`](scripts/action_perturbation.py)；
- 训练入口：[`scripts/train_phase1.py`](scripts/train_phase1.py)。
- 文件保留范围与清理记录：[`项目文件清理说明.md`](项目文件清理说明.md)。

### 7.1 与报告结果对应的可视化协议

[`scripts/visualize.py`](scripts/visualize.py) 提供四个具名预设，避免回放时遗漏正式实验中的扰动参数：

| 可视化预设 | 对应结果 | 孔位误差 | 动作噪声 | 增益扰动 | 教师门限 | 深度屏障 | episode |
|---|---|---:|---:|---:|---:|---:|---:|
| `task1_basic` | 任务1基础 512/512 | 无固定偏差；保留训练时0.5 mm/0.25°均匀观测噪声 | 0 | 0 | 1 mm | 38 mm | 20 s |
| `task1_stress` | 任务1 PPO 124/128 | 每回合固定 XY 标准差0.5 mm | 0.02 | 5% | 1 mm | 关闭 | 20 s |
| `task2_basic` | 任务2基础 512/512 | 无固定偏差；保留训练时0.5 mm/0.25°均匀观测噪声 | 0 | 0 | 1 mm | 关闭 | 20 s |
| `task2_fair` | 任务2 PPO 128/128、教师110/128 | 每回合固定 XY 标准差0.5 mm | 0.0305 | 5% | 2 mm | 关闭 | 20 s |

四个预设的动作延迟均为0。可视化首先在无界面的批量环境中按对应协议运行策略，只保存满足严格成功判据的真实轨迹；随后 GUI 仅回放已记录的关节和夹具状态。因此，画面中的轨迹已经包含扰动造成的运动结果，但 GUI 回放阶段不会再次随机施加一遍扰动。

每条持久化轨迹旁边的同名 JSON 会记录完整协议；轨迹内部还记录该成功 episode 实际采样的孔位偏差、动作增益和末步动作噪声。

`--show_fixture_plate` 会在 GUI 中持续显示固定夹具：任务1显示带圆孔的方形孔板，任务2显示六个固定孔套筒。该几何仅用于回放显示，不改变已验证轨迹的接触物理。回放器会把并行采集环境中的夹具位姿转换到 GUI 单环境坐标系，并在每一帧场景同步后恢复显示，因此孔板/孔套筒不会在机械臂运动过程中消失。

以下命令均应先进入项目使用的环境：

```bash
conda activate isaac_lab
cd /path/to/Franka_FR3_Peg_in_Hole
```

任务1基础协议：

```bash
OMNI_KIT_ACCEPT_EULA=YES python scripts/visualize.py \
  --protocol task1_basic --episodes 3 --capture_num_envs 64 \
  --show_fixture_plate \
  --screenshot_dir artifacts/task1_visualization/basic \
  --trajectory_output artifacts/task1_visualization/task1_basic.pt
```

任务1最终强扰动协议：

```bash
OMNI_KIT_ACCEPT_EULA=YES python scripts/visualize.py \
  --protocol task1_stress --episodes 3 --capture_num_envs 64 \
  --show_fixture_plate \
  --screenshot_dir artifacts/task1_visualization/stress \
  --trajectory_output artifacts/task1_visualization/task1_stress.pt
```

任务2基础协议：

```bash
OMNI_KIT_ACCEPT_EULA=YES python scripts/visualize.py \
  --protocol task2_basic --episodes 3 --capture_num_envs 64 \
  --show_fixture_plate \
  --screenshot_dir artifacts/task2_visualization/basic \
  --trajectory_output artifacts/task2_visualization/task2_basic.pt
```

任务2最终公平强扰动协议（目标孔按正式评估方式随机选择）：

```bash
OMNI_KIT_ACCEPT_EULA=YES python scripts/visualize.py \
  --protocol task2_fair --episodes 3 --capture_num_envs 64 \
  --show_fixture_plate \
  --screenshot_dir artifacts/task2_visualization/fair \
  --trajectory_output artifacts/task2_visualization/task2_fair.pt
```

任务2六个目标孔逐孔回放（每个孔均保留最终公平协议扰动）：

```bash
OMNI_KIT_ACCEPT_EULA=YES python scripts/visualize_task2_six_holes.py \
  --protocol task2_fair --capture_num_envs 128 \
  --show_fixture_plate \
  --screenshot_dir artifacts/task2_visualization/fair_six_holes \
  --trajectory_output artifacts/task2_visualization/task2_fair_six_holes.pt
```

重新生成任务2布局图、任务2公平对照图，以及任务1/任务2全部最终结果总览图：

```bash
python scripts/plot_task2_visualization.py \
  --output-dir artifacts/task2_visualization
```

## 八、最终评价

本项目最终形成了一套适合接触丰富插入任务的方法论：先用物理几何和解析控制器确认任务可达，再用相对观测和明确噪声建模满足信息约束，最后冻结解析教师并通过 PPO 学习有界残差。

最终结果表明：

- 任务1满足老师提出的单孔随机插入要求；
- 任务2满足六孔阵列、目标孔随机和阵列整体随机要求；
- 两个任务均使用了 RL，但 RL 的角色是教师附近的残差适应，而不是从零探索；
- 在最终任务2公平强扰动协议下，PPO 从先前的 50.78% 提升到 100%，超过几何教师的 85.94%；
- 当前仿真阶段的主要目标已经完成，后续若继续推进，应转向更高保真动力学、真实 FR3 接口和实机验证，而不是继续改变任务2主控制逻辑。

早期 `1.56%/2.34%` 结果保留为历史诊断证据，但不属于最终验收结果。
