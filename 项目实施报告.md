# 基于几何教师与残差强化学习的 Franka 机械臂插孔任务实施报告


## 摘要

本文面向 Franka 机械臂圆棒插孔任务，完成了从单孔随机插入到 `2 × 3` 六孔阵列指定孔插入的物理仿真、控制策略训练、统一评估和可视化验证。任务要求将直径 20 mm 的圆柱棒插入直径 23 mm 的孔中，同时考虑目标孔在约 `10 cm × 10 cm` 工作空间内的随机初始化、棒相对末端法兰的小幅安装不确定性，以及机器人不能直接获得无噪声绝对棒/孔位姿等约束。

针对接触丰富任务中纯强化学习探索效率低、训练结果难以审计的问题，项目采用“几何教师 + 有界 residual PPO”的分层方法。首先利用棒尖到目标孔的相对位移、插入深度和姿态误差构造解析几何教师，通过 6D Operational Space Control（OSC）完成接近、对准和插入；随后冻结几何教师，由 PPO 仅学习幅值受限的残差动作，以补偿孔位估计偏差、每步动作噪声和每回合控制增益扰动。任务2额外引入目标孔 one-hot 条件，并保留所有六个孔的碰撞几何。

实验结果表明：任务1在基础协议下达到 `512/512 = 100%`；在每回合固定 XY 标准差 0.5 mm 的孔位估计偏差、动作噪声 0.02 和 5% 增益扰动下，几何教师成功率为 `92.97%`，500轮 residual PPO 提升到 `96.88%`。任务2在基础六孔协议下达到 `512/512 = 100%`；在动作噪声增强至 0.0305 的最终公平协议下，几何教师为 `85.94%`，改进后的 residual PPO 达到 `128/128 = 100%`，提升 14.06 个百分点。结果验证了几何先验与强化学习局部适应相结合的方法在高精度插入任务中的有效性。

**关键词：** Franka机械臂；插孔装配；Isaac Lab；操作空间控制；几何教师；残差强化学习；PPO；域随机化

## 1. 引言

### 1.1 项目背景

插孔装配是机器人精密操作中的典型任务。与自由空间抓取不同，插入过程同时受到几何间隙、末端姿态、接触摩擦、执行误差和观测误差影响。当棒与孔之间的径向间隙较小时，几毫米的目标估计偏差或轻微姿态倾斜都可能导致卡滞、超深插入或超时。

本项目的原始要求包括两个逐步增加难度的任务：

1. 在物理仿真中控制 Franka 机械臂，将直径约 2 cm 的棒插入直径约 2.3 cm 的孔；孔在约 `10 cm × 10 cm` 范围内随机，棒相对 flange 存在约 ±5 mm 抓取不确定性，成功率达到 90%以上；
2. 将单孔扩展为 `2 × 3` 六孔阵列，孔间距 3 cm，阵列整体仍在 `10 cm × 10 cm` 范围内随机，并能够对任意指定目标孔完成插入。

### 1.2 主要挑战

本项目包含以下关键挑战：

- **高精度要求。** 棒直径 20 mm、孔直径 23 mm，径向总间隙有限；
- **接触丰富动力学。** 插入动作不仅要求位置对准，还要求棒轴保持近似竖直；
- **观测受限。** 策略不能直接使用无噪声绝对孔位姿，需要基于带误差的相对观测；
- **抓取和执行不确定性。** Peg mount、动作和控制增益均存在扰动；
- **多目标条件化。** 任务2必须根据目标孔编号在六个共存碰撞体中选择正确孔；
- **评估一致性。** 不同时间预算、门限、扰动或环境 wrapper 会显著改变成功率，必须建立公平协议。

### 1.3 本项目的主要工作

项目完成了以下实施工作：

1. 构建单孔和六孔阵列的碰撞几何、随机化事件、观测、奖励与严格终止条件；
2. 建立 6D Pose OSC 和解析几何教师，先验证任务的物理可达性；
3. 构建相对观测、孔位估计偏差、动作噪声和控制增益扰动模型；
4. 实现冻结教师上的有界 residual actor，以及带教师残差惩罚的 PPO；
5. 统一训练与评估协议，修正任务2早期低成功率所暴露的时间预算和门限问题；
6. 完成任务1、任务2批量评估、逐孔验证、结果图表与 Isaac Sim GUI 回放。

## 2. 任务建模与问题定义

### 2.1 机器人与插孔几何

仿真使用 Franka 7自由度机械臂开发模型，末端安装长度 100 mm、半径 10 mm 的圆柱 Peg。Peg 通过两个 X/Y 方向的 mount 自由度建模抓取不确定性，关节限位为 ±5 mm。开发资产见 [assets/panda_with_fixed_peg.usda](assets/panda_with_fixed_peg.usda)。

单孔和阵列孔的目标开口直径为 23 mm。圆孔碰撞边界由 36 段环形壁组成，以避免使用仅有视觉效果、没有真实碰撞约束的孔模型。

需要说明的是，当前本地资产以 Isaac Lab Franka Panda USD 为基础构建，用作正式 FR3 模型导入前的动力学开发代理。因此本文的“Franka FR3任务”是面向 FR3 应用目标的仿真实施，后续实机阶段仍需替换和校准正式 FR3 描述文件。

### 2.2 任务1随机化

任务1的目标孔在约 `10 cm × 10 cm` 可达范围内随机。除物理孔位置随机化外，压力测试还在策略观测端为每个 episode 采样一次固定 XY 偏差：

$$
\boldsymbol{b}_{xy} \sim \mathcal{N}(\boldsymbol{0}, \sigma_h^2 \boldsymbol{I}),
\qquad \sigma_h = 0.5\ \text{mm}.
$$

该偏差在一个 episode 内保持不变，更接近孔位标定误差或视觉估计系统偏差，而不是逐帧白噪声。

### 2.3 任务2六孔阵列

任务2包含 2行3列共6个孔，孔中心间距为 30 mm。六孔相对位置保持不变，阵列原点整体在约 `10 cm × 10 cm` 范围内随机。

![任务2六孔阵列布局](artifacts/task2_visualization/task2_array_layout.png)

*图1  六孔阵列、孔编号及阵列整体随机范围。*

每个 episode 选择一个目标孔 $i \in \{0,1,\ldots,5\}$。目标编号以 6 维 one-hot 向量输入策略。任务2并不是在一个 episode 中依次插完六个孔，而是在所有孔同时存在的情况下，对随机或指定目标孔完成一次严格插入；逐孔可视化则分别采集六个目标的成功 episode。

### 2.4 观测与动作空间

任务1策略观测记为：

$$
\boldsymbol{o}_t =
[\boldsymbol{q}_t,\dot{\boldsymbol{q}}_t,
\Delta\boldsymbol{p}_{\text{peg-hole},t},
\boldsymbol{e}_{\text{tilt},t},
\boldsymbol{a}_{t-1}],
$$

其中：

- $\boldsymbol{q}_t \in \mathbb{R}^{9}$：关节位置；
- $\dot{\boldsymbol{q}}_t \in \mathbb{R}^{9}$：关节速度；
- $\Delta\boldsymbol{p}_{\text{peg-hole},t} \in \mathbb{R}^{3}$：棒尖到目标孔的相对位移；
- $\boldsymbol{e}_{\text{tilt},t} \in \mathbb{R}^{3}$：棒轴倾斜信息；
- $\boldsymbol{a}_{t-1} \in \mathbb{R}^{6}$：上一控制动作。

因此任务1观测维度为30。任务2增加目标孔 one-hot $\boldsymbol{c}_i \in \mathbb{R}^{6}$，总观测维度为36。

策略不读取隐藏的 Peg mount 关节，也不直接读取无噪声绝对孔位姿。动作 $\boldsymbol{a}_t \in \mathbb{R}^{6}$ 为末端相对位姿命令，包括三维平移和三维旋转修正，由 6D OSC 映射到关节力矩/动作目标。

### 2.5 成功判据

设终端径向误差为 $r$，插入深度为 $d$，棒倾角为 $\theta$。最终成功事件定义为：

$$
\mathcal{S} =
\{r \leq 1.3\ \text{mm}\}
\cap \{15\ \text{mm} \leq d \leq 40\ \text{mm}\}
\cap \{\theta \leq 2^\circ\}
\cap \{\text{no workspace violation}\}.
$$

每个 episode 的时间预算为20秒，即600个30 Hz控制步。`success`、`timeout`、`over-insertion` 和其他失败原因分别统计，任何一般 termination 都不能直接视为成功。

## 3. 系统方法

### 3.1 6D Operational Space Control

物理仿真频率为120 Hz，控制 decimation 为4，因此策略和 OSC 的控制频率为30 Hz。控制器同时调节末端三维位置和三维姿态，最终零空间刚度设置为20。

统一物理参数包括：

| 参数 | 设置 |
|---|---:|
| physics time step | `1/120 s` |
| decimation | 4 |
| policy time step | `1/30 s` |
| position solver iterations | 16 |
| velocity solver iterations | 4 |
| enhanced determinism | 开启 |

项目早期曾使用不完整位置控制和不同初始姿态。通过雅可比条件、腕部姿态与 Peg 垂直度审计后，最终固定为全6轴 Pose OSC，以避免腕部冗余漂移导致的棒尖 XY 扫动。

### 3.2 三阶段几何教师

几何教师根据带误差的相对观测生成动作，并将插入过程划分为三个阶段：

1. **Approach：** 在孔上方保持安全高度，优先消除较大 XY 误差；
2. **Align：** 接近孔口后降低平移速度，进一步校正径向误差和棒轴姿态；
3. **Insert：** 当径向误差和倾角进入门限后，沿孔轴方向插入。

记测得的 XY 相对误差为 $\tilde{\boldsymbol{e}}_{xy}$，姿态误差为 $\tilde{\boldsymbol{e}}_R$，教师动作可概括为：

$$
\boldsymbol{a}^{T}_t =
\operatorname{clip}
\left(
\begin{bmatrix}
k_p\tilde{\boldsymbol{e}}_{xy} \\
v_z(\tilde{r},d) \\
k_R\tilde{\boldsymbol{e}}_R
\end{bmatrix},-1,1
\right),
$$

其中 $v_z$ 由当前阶段、径向门限和插入深度决定。任务1最终教师 alignment gate 为1 mm，任务2公平协议放宽为2 mm，以适应阵列目标条件、较长水平路径和更强动作扰动。

几何教师的实现见 [scripts/geometric_teacher.py](scripts/geometric_teacher.py)。

### 3.3 有界 residual 策略

最终策略由冻结教师和可训练残差共同构成：

$$
\boldsymbol{a}_t =
\operatorname{clip}
\left(
\boldsymbol{a}^{T}_t +
\alpha\boldsymbol{a}^{R}_{\phi}(\boldsymbol{o}_t),
-1,1
\right),
$$

其中 $\boldsymbol{a}^{R}_{\phi}$ 是两层 `64-64-Tanh` residual head，$\alpha=0.15$ 为残差缩放系数。教师参数冻结，PPO 只需学习教师附近的局部修正。

这种结构具有三点优势：

- 初始策略已经具备完整接近—对准—插入能力；
- residual 幅值受限，降低随机探索破坏接触过程的风险；
- PPO 的性能提升可以解释为对扰动和模型误差的补偿，而不是不可审计的完整动作重构。

### 3.4 Residual PPO目标

在标准 PPO clipped surrogate、价值损失和熵项基础上，训练加入教师动作残差惩罚。在最小化形式下，总损失写为：

$$
\mathcal{L}_{\text{total}}
= \mathcal{L}_{\text{PPO}}
+ \lambda_{R}
\mathbb{E}_t
\left[
\|\boldsymbol{a}_t-\boldsymbol{a}^{T}_t\|_2^2
\right],
$$

最终 $\lambda_R=100$。较强的残差惩罚配合较低学习率，限制 PPO 在接触阶段发生过大的策略漂移。

代码实现见 [scripts/residual_policy.py](scripts/residual_policy.py) 和 [scripts/residual_ppo.py](scripts/residual_ppo.py)。

### 3.5 扰动模型

最终实验区分以下扰动：

1. **教师均匀观测噪声：** 训练基础模型时对相对位置和倾角逐步加噪；
2. **episode固定孔位估计偏差：** 每回合采样一次 $\boldsymbol{b}_{xy}$，回合内保持不变；
3. **动作噪声：** 每控制步加入零均值高斯动作噪声；
4. **动作增益扰动：** 每回合采样一个控制增益系数；
5. **Peg mount不确定性：** X/Y安装偏差由物理模型建模。

动作扰动可写为：

$$
\tilde{\boldsymbol{a}}_t
= g_e\boldsymbol{a}_t + \boldsymbol{\epsilon}_t,
\quad
g_e \sim \mathcal{N}(1,0.05^2),
\quad
\boldsymbol{\epsilon}_t \sim \mathcal{N}(0,\sigma_a^2\boldsymbol{I}).
$$

所有最终主线协议的动作延迟均为0。老师任务没有提出延迟噪声要求，且实验显示当前6D OSC对控制延迟高度敏感，因此没有把延迟作为最终验收扰动。

## 4. 项目实施过程

### 4.1 阶段一：环境、几何与终止条件审计

项目首先完成单孔环境的几何与动力学审计，包括：

- 校准机器人初始姿态与棒尖位置；
- 将 Peg 作为机器人动力学链中的固定/受限安装部件，而不是每帧强制同步的独立物体；
- 使用36段实体孔壁建立可接触孔口；
- 检查插入深度符号、孔口表面高度与棒尖定义；
- 将 `success`、`timeout`、`over-insertion` 和 workspace violation 分离；
- 统一120 Hz物理和30 Hz控制设置。

这一阶段的目标不是立即训练 RL，而是先回答“当前机器人、孔壁和控制器是否能在物理上完成任务”。

### 4.2 阶段二：几何教师与基础单孔验收

在几何与终止条件稳定后，项目实现三阶段几何教师，并在无强扰动和弱观测噪声下进行批量验证。最终23 mm孔、约 `10 cm × 10 cm` 随机范围下，基础任务达到 `512/512 = 100%`。

几何教师在基础协议中已经达到100%，说明继续在简单协议上训练 PPO 无法体现强化学习价值。因此项目没有把“PPO同样达到100%”作为唯一结论，而是进一步构造能够区分教师与 residual PPO 的压力协议。

### 4.3 阶段三：任务1强扰动与500轮PPO

最初考虑过1 mm孔位误差，但该值相对1.5 mm径向总间隙过强，容易让任务退化为系统性不可观测偏差。最终选择每个episode固定 XY 标准差0.5 mm，同时加入动作噪声0.02和5%增益扰动。

该协议将几何教师成功率降低到约93%，为 PPO 留出了可观察的改进空间。随后将训练轮数增加到500轮，并采用较保守的更新配置：学习率 `1e-5`、单轮 PPO epoch、初始噪声0.03、entropy 0、residual penalty 100。最终 PPO 达到96.88%。

### 4.4 阶段四：六孔阵列构建与早期失败

任务2增加了6个真实孔壁、阵列整体随机、目标孔选择和one-hot条件。早期实验曾出现 `1.56%/2.34%` 的极低成功率。排查表明，这一结果不代表六孔物理不可达，主要由以下协议问题导致：

- 默认episode只有12秒，即约360步；
- 教师 alignment gate 沿用任务1的1 mm；
- 不同评估入口对环境reset、观测和OSC更新的处理不一致；
- 早期参数直接沿用无扰动单孔设置，没有针对六孔较长水平路径调节。

将时间预算统一为20秒、教师门限改为2 mm，并使用正式 wrapper 评估后，六孔随机阵列的物理可达性得到验证，基础 PPO 达到 `512/512 = 100%`。

### 4.5 阶段五：任务2公平协议与重新训练

为公平比较几何教师和 PPO，任务2最终协议统一使用：

- 128个episode、64个并行环境、seed 42；
- 20秒/600步；
- XY孔位估计偏差标准差0.5 mm；
- 动作噪声0.0305；
- 控制增益扰动5%；
- 动作延迟0；
- 教师 alignment gate 2 mm；
- 零空间刚度20；
- 深度安全屏障关闭。

改进前任务2 PPO 使用较高学习率、较弱初始探索和较低残差惩罚，在该协议下只能达到约50.78%。最终训练改用任务1已经验证的保守更新配置，训练500轮后达到100%。

### 4.6 阶段六：独立评估、证据固化与可视化

最终 PPO 评估统一使用 [scripts/eval_checkpoint_simple.py](scripts/eval_checkpoint_simple.py) 中的 `RslRlVecEnvWrapper` 路径。直接 raw-env 的实验入口曾在第二个OSC控制步触发Isaac底层提前退出，因此不作为正式结果来源。

项目将最终协议、训练配置、checkpoint、逐episode结果和图表分别保存到：

- `runs/ppo/`：训练配置、TensorBoard事件和模型；
- `runs/eval/`：正式评估JSON和诊断报告；
- `artifacts/`：结果图、GUI首末帧截图和可复用轨迹。

GUI可视化采用“无头真实采集 + 新进程状态回放”：先按正式扰动协议真实运行策略，仅保存严格成功轨迹，再在GUI中回放记录状态。这样既保留了扰动对运动的影响，也避免GUI阶段重复施加一遍随机扰动。

## 5. 实验设置

### 5.1 软件与硬件

| 项目 | 配置 |
|---|---|
| 操作系统 | Ubuntu 22.04.5 LTS |
| GPU | NVIDIA RTX 5880 Ada，49 GB VRAM |
| NVIDIA驱动 | 580.173.02 |
| Python | 3.10.20 |
| Isaac Sim | 4.5.0 |
| Isaac Lab | 2.1.0 |
| PyTorch | 2.5.1 |
| Gymnasium | 1.3.0 |
| RSL-RL | 2.3.3 |

### 5.2 最终训练超参数

| 参数 | 任务1最终训练 | 任务2最终训练 |
|---|---:|---:|
| 并行环境 | 1024 | 1024 |
| 训练轮数 | 500 | 500 |
| rollout | 64 steps/env | 64 steps/env |
| learning rate | `1e-5` | `1e-5` |
| PPO epochs | 1 | 1 |
| 初始策略噪声 | 0.03 | 0.03 |
| entropy coefficient | 0 | 0 |
| residual penalty | 100 | 100 |
| residual scale | 0.15 | 0.15 |
| 训练动作噪声 | 0.02 | 0.02 |
| 增益扰动 | 5% | 5% |
| teacher gate | 1 mm | 2 mm |
| reset warmup | 20步 | 0步 |

任务2训练动作噪声为0.02，但最终公平评估使用更强的0.0305。二者属于不同阶段的协议，不能混淆。

### 5.3 正式评估协议

| 协议 | episode | 孔位误差 | 动作噪声 | 增益扰动 | 延迟 | 教师门限 |
|---|---:|---:|---:|---:|---:|---:|
| 任务1基础 | 512 | 0.5 mm/0.25°逐步均匀教师观测噪声 | 0 | 0 | 0 | 1 mm |
| 任务1强扰动 | 128 | episode固定XY标准差0.5 mm | 0.02 | 5% | 0 | 1 mm |
| 任务2基础 | 512 | 0.5 mm/0.25°逐步均匀教师观测噪声 | 0 | 0 | 0 | 1 mm |
| 任务2公平强扰动 | 128 | episode固定XY标准差0.5 mm | 0.0305 | 5% | 0 | 2 mm |

所有正式协议均使用统一成功判据。任务1强扰动和任务2公平协议使用 `num_envs=64`、seed 42。

## 6. 实验结果

### 6.1 总体结果

![任务1与任务2结果总览](artifacts/task2_visualization/final_results_summary.png)

*图2  任务1和任务2基础协议及强扰动协议结果总览。*

| 任务 | 方法 | 成功 | timeout | over-insertion | 成功率 |
|---|---|---:|---:|---:|---:|
| 任务1基础 | 几何教师 | 512/512 | 0 | 0 | 100% |
| 任务1基础 | Residual PPO | 512/512 | 0 | 0 | 100% |
| 任务1强扰动 | 几何教师 | 119/128 | 9 | 0 | 92.97% |
| 任务1强扰动 | Residual PPO 500轮 | 124/128 | 4 | 0 | 96.88% |
| 任务2基础 | Residual PPO | 512/512 | 0 | 0 | 100% |
| 任务2公平强扰动 | 几何教师 | 110/128 | 18 | 0 | 85.94% |
| 任务2公平强扰动 | Residual PPO 500轮 | 128/128 | 0 | 0 | 100% |

### 6.2 任务1结果

任务1基础协议中，几何教师和 residual PPO 均为100%。在强扰动协议中，PPO将失败数从9个减少到4个，绝对提升：

$$
\Delta S_1 = \frac{124-119}{128}\times100\%
= 3.91\ \text{percentage points}.
$$

所有失败均为timeout，没有发生over-insertion。这说明当前主要瓶颈是扰动下的有限时间对准，而不是深度安全失效。

![任务1强扰动插入末帧](artifacts/task1_visualization/stress/episode_1_final.png)

*图3  任务1强扰动成功轨迹末帧；固定孔板和孔口在机械臂运动期间持续显示。*

任务1完整结果见 [runs/eval/stress_rl_gain_hole05_bias_500_summary.md](runs/eval/stress_rl_gain_hole05_bias_500_summary.md)。

### 6.3 任务2结果

任务2最终公平协议中，几何教师为 `110/128`，PPO为 `128/128`。绝对提升：

$$
\Delta S_2 = \frac{128-110}{128}\times100\%
= 14.06\ \text{percentage points}.
$$

![任务2公平协议成功率](artifacts/task2_visualization/task2_success_summary.png)

*图4  任务2基础PPO、最终公平几何教师与最终公平residual PPO对比。*

最终PPO全部128个episode均满足成功判据，其终端指标如下：

| 指标 | 均值 | 最小值 | 最大值 | 成功门限 |
|---|---:|---:|---:|---:|
| 插入深度 | 16.461 mm | 15.012 mm | 26.465 mm | 15–40 mm |
| 径向误差 | 0.836 mm | 0.084 mm | 1.294 mm | ≤1.3 mm |
| 棒倾角 | 0.736° | 0.153° | 1.612° | ≤2° |

最大径向误差1.294 mm接近1.3 mm严格门限，但仍在允许范围内；最大倾角1.612°与最大深度26.465 mm均保留安全裕度。

![任务2六孔插入末帧](artifacts/task2_visualization/six_holes/episode_6_final.png)

*图5  六个目标孔逐孔验证中的一个成功末帧；所有非目标孔仍保留在场景中。*


### 6.4 任务2训练改进对照

| 训练方案 | 学习率 | PPO epochs | 初始噪声 | residual penalty | 公平协议结果 |
|---|---:|---:|---:|---:|---:|
| 改进前方案 | `1e-4` | 2 | 0.005 | 10 | 约50.78% |
| 最终方案 | `1e-5` | 1 | 0.03 | 100 | 100% |

该对照说明，在已有强几何教师的前提下，过快更新和较弱约束会破坏教师附近的稳定行为；降低学习率、减少每轮更新次数并加强残差约束后，策略更适合学习小幅但有效的扰动补偿。

## 7. 结果分析与讨论

### 7.1 为什么基础教师已经100%仍需要RL

基础协议中几何教师达到100%，只能说明在较弱扰动下解析控制足够稳定，不能证明其对未建模误差具有最优鲁棒性。通过增强孔位估计偏差、动作噪声和增益扰动，任务1教师下降到92.97%，任务2教师下降到85.94%，此时PPO分别提升到96.88%和100%。

因此，本项目中RL的意义不是替代几何知识，而是学习解析模型难以精确描述的局部补偿。这种角色分工与结果相符：教师负责可达性和主动作方向，PPO负责对系统性偏差与随机执行误差进行适应。

### 7.2 为什么任务2不是任务1的简单重复

如果任务2只是把任务1独立运行六次，那么每次插入的动力学和观测结构应完全相同。但实际任务2增加了：

- 目标孔one-hot条件；
- 阵列整体随机位姿；
- 六个孔同时存在的碰撞环境；
- 不同目标孔对应的可达方向和水平路径长度；
- 自动reset后的目标孔重选与阵列同步。

因此任务2需要学习条件策略 $\pi(\boldsymbol{a}|\boldsymbol{o},\boldsymbol{c}_i)$，而不是复制一个无条件单孔策略。早期低成功率也表明，单孔时间预算和门限不能不加审计地直接沿用。

### 7.3 公平比较的重要性

本项目的排查表明，下列任一变化都可能显著影响成功率：

- episode由12秒改为20秒；
- teacher gate由1 mm改为2 mm；
- 动作噪声由0.02改为0.0305；
- 是否使用reset warmup；
- 是否使用 `RslRlVecEnvWrapper`；
- 是否错误地把一般termination计为成功。

因此最终结论仅在明确记录的同一协议内比较教师和PPO。任务2的85.94%与100%使用相同seed、时间预算、孔位偏差、动作噪声、增益扰动和成功判据，具备直接比较意义。

### 7.4 失败模式

任务1和任务2最终对照中的教师失败均主要表现为timeout，over-insertion为0。说明当前深度约束和插入阶段设计有效，主要剩余问题是强扰动下无法在时间预算内完成对准。

PPO减少timeout，表明residual动作主要改善了接近末期和孔口附近的收敛效率。任务2PPO没有以更深插入或更大倾角换取成功：其最大深度、径向误差和倾角仍满足严格门限。

### 7.5 方法优势

- **样本效率较高：** 策略从可工作的教师附近开始，而非随机探索；
- **安全性较好：** residual幅值和教师偏离均受约束；
- **可解释：** 可以分别评估教师、残差和扰动协议；
- **便于迁移：** 单孔验证过的保守PPO配置能够迁移到六孔任务；
- **可复现：** 训练配置、checkpoint、评估JSON和可视化轨迹均已固化。

### 7.6 局限性

尽管仿真结果达到任务目标，仍存在以下局限：

1. **机器人模型限制。** 当前使用Isaac Lab Franka Panda资产作为FR3开发代理，尚未完成正式FR3模型校准；
2. **仅仿真验证。** 摩擦、结构柔性、关节回差、传感器时延和真实接触噪声仍与实机存在差异；
3. **感知链路未完整建模。** 项目直接向策略提供带误差的相对位移，而非从相机图像端到端估计；
4. **最终协议不含动作延迟。** 这是根据老师任务范围和控制器敏感性做出的选择，不代表系统具有时延鲁棒性；
5. **正式结果主要使用单一seed 42。** 后续学术发表应增加多seed训练与置信区间；
6. **未进行实机安全验证。** 当前策略不能直接下发真实机器人。


## 8. 结论

本项目完成了老师要求的两个仿真任务：

1. 单孔任务实现了23 mm随机孔、Peg安装不确定性、受限相对观测和超过90%的成功率；
2. 六孔任务实现了2×3阵列、30 mm孔间距、阵列整体随机和任意目标孔条件插入。

方法上，项目最终采用“物理几何审计—解析教师验证—显式扰动建模—冻结教师上的有界residual PPO—统一协议评估”的路线。该方法避免了直接从零RL在接触任务中的低效率和不稳定性，同时使RL性能增益能够在可解释的教师基线上测量。

实验表明，任务1 residual PPO在强扰动下由教师的92.97%提升到96.88%；任务2在更强公平协议下由教师的85.94%提升到100%。这说明PPO确实学习到了几何教师之外的扰动补偿，而不是仅重复教师行为。

综上，项目已完成当前Isaac Lab仿真阶段的任务验收。下一阶段更有价值的工作不是继续修改主控制逻辑，而是导入正式FR3模型、构建视觉/力觉感知链、加强接触动力学随机化，并按照低速、低力、分阶段原则开展实机验证。

